package kst4contest.view;

/**
 * Main class with simple main method.
 * Workaround to avoid Error that JavaFX is missing.
 */
public class Main {
    /**
     * Main method that simply calls the main method inside our application class.
     * @param args Commandline Arguments.
     */
    public static void main(String[] args) {
        Kst4ContestApplication.main(args);
    }
}
